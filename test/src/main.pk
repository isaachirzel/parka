function action() -> i32
{
	return 12 * 4;
}

function fibonacci(a: i32) -> i32
{
	var zero: i32 = 0;
	var one: i32 = 1;
	
	if a <= zero
		return 0;

	if a <= one
		return 1;

	var t1 = 0;
	var t2 = 1;
	var next = t1 + t2;

	for var i: i32 = 3; i < a; i = i + 1
	{
		t1 = t2;
		t2 = next;
		next = t1 + t2;
	}

	return next;
}

function notImplemented();

function main() -> i32
{
	var a = fibonacci(1);

	return a;
}
